Multi-line text area; same label/helper/error system as Input.

```jsx
<Textarea label="Notes" placeholder="Installation notes…" rows={4} />
<Textarea label="Description" error="Required field" />
```

Min height 96px, vertically resizable. Focus blue ring; `error` turns it red.
