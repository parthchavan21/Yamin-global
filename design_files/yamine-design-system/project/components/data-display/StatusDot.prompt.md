Minimal live-status dot with optional label and pulse.

```jsx
<StatusDot status="online" pulse>Streaming</StatusDot>
<StatusDot status="offline">Offline</StatusDot>
```
Use inside tables, list rows and headers where a full Badge is too heavy.
